---
version: '3'
services:
  web:
    build: tomcat/
    container_name: tomcat
    ports:
      - "8080:8080"
    depends_on:
      - backend
    volumes:
      - temp:/opt/tomcat/webapps

  backend:
    build: maven/
    container_name: maven
    volumes:
      - temp:/opt/boxfuse/target
volumes:
  temp: